function pswdReset(form) {
    const pswd = form.reset-input-password.value;
    const cpswd = reset-input-con-password.value;

    //your password reset function
    /*if (passwordHasBeenReseted) {
        emailSend();
    } else {
        alert(error);
    }*/

    return false;
}

function emailSend() {
    //Password has been reseted
    //Send email about the password change
}